let isloading = false;
let nombre = "";

const llamarPersonajes = async(page = 1,limit = 8) => {
    try{

        const response =  await fetch('');
    }catch(error){
        throw new Error(error);
    }
    }
